import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Placeholder - auth requires .env and network, so we skip
    // full widget tests for now.
    expect(true, isTrue);
  });
}
